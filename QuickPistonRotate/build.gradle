plugins {
    id 'java-library'
    id 'net.neoforged.moddev' version '2.0.78'
}

version = '1.0.0'
group = 'com.example.quickpiston'

neoForge {
    version = '21.1.60'

    // Desactive Parchment pour utiliser les mappings officiels par defaut
    // parchment {
    //     mappingsVersion = '2024.07.28'
    //     minecraftVersion = '1.21.1'
    // }

    runs {
        client {
            client()
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Les dépendances Create peuvent être ajoutées en 'compileOnly' si besoin
}